const styles = theme => ({
  textField: {
    marginLeft: theme.spacing.unit,
    marginRight: theme.spacing.unit,
    width: 200,
  },
  refreshWebsite: {
    backgroundColor: "red",
  }
});

export default styles;